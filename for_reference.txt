// app.method(url , ()) -> handle method request when on url

/* app.post('/flashcards', async (req, res) => {
    const { word } = req.body;
  
    if (!word) {
      return res.status(400).send('Word parameter is required');
    }
  
    try {
      const response = await axios.get(api_url + word);
      const flashcardData = response.data;
      const meanings = flashcardData.map(entry => entry.meanings).flat();

    res.json({ meanings });
    } catch (error) {
      res.status(500).send(error.message);
    }
  });
   */

/* app.get("/",(req,res)=>{
    res.send(getData())
})

//async function to extract data
const getData = async ()=> {
    const response = await fetch(api_url , {method: 'GET'});



if (!response.ok) {
    throw new Error(`status: ${response.status}`);
}
const data = await response.json();
return data
}

//async function to present in flashcard format

const createFlashcards = async ()=>{
    const flashcards = []
    const description = await getData(word)
    flashcards.push({
        word,
        description,
    }
    )

    return flashcards
}

const main = async ()=>{
    const flashcards = await createFlashcards();
    console.log(JSON.stringify(flashcards, null, 2))
} 

main()
 */






 /*
 
// /* algo:
// 1) fetch data from api and store
// 2) separate required data from fetched data
// 3) present as a flashcard 
// */

// import fetch from 'node-fetch';
// import express from 'express'

// const app = express();

// app.use(express.json())

// app.listen(5000,()=>{
//     console.log("Server is running")
// })


// const word ="home"

// const api_url = 'https://api.dictionaryapi.dev/api/v2/entries/en/' + word

// const fetch_data = async()=>{
//   const response = await fetch(api_url , {method:'GET'});
//   const data = await response.json();
//   return data;
// }

// const data_to_flashcard = async()=>{
//   const flashcard = [];
//   const information = await fetch_data(word)
//   const definition = information[0].meanings[0].definitions[0].definition
//   flashcard.push({
//     word,
//     definition,
//   })
//   return flashcard;
  
// }

// const flashcards = await data_to_flashcard();
// console.log(JSON.stringify(flashcards,null,2))

// app.get('/', (req, res) => {
//   res.setHeader('Content-Type', 'application/json');
//   res.status(200).json(flashcards);
// });
 */




